/**
 * Smart Campus Operations Hub – root package (IT3030 PAF).
 *
 * <p><b>Structure:</b> {@code *Controller} = REST layer; {@code *Service} = rules + notifications;
 * MongoDB = users, bookings, tickets, resources, notifications; JPA/H2 = {@code recent_activities}.</p>
 *
 * <p><b>See also:</b> {@code config} (security, OAuth), {@code user}, {@code booking}, {@code ticket},
 * {@code notification}.</p>
 */
package com.sliit.backend;
